
Implement special moves (Promotion, Castle, ...):
    - Implement regex for them (in isWellformedMoveDescription()).
    - Implement actual move.

(Check string input for check(mate) if it occurs.)

Prevent king suicide.

Implement checking for x if capture is made.

Check if rank, file aren't null in Pieces constructors.