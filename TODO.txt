
Make it possible to convert char/String to File (a -> enum a). Change code in Game to reflect change (currently fails during runtime).

Implement special moves (Promotion, Castle, ...):
    - Implement regex for them (in isWellformedMoveDescription()).
    - Implement actual move.

Implement checking for check/checkmate.

Eating of pieces.

Use File and Rank instead of Square to remember position (multiple places)?